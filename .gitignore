# Compiler files
cache/
out/

# Ignores development environments
.env
.DS_Store
DEPLOY.md
script/
docs/
# Forge dependencies
# We can include lib in .gitignore or keep it depending on whether we want to version control deps.
# Standard practice is often to track submodules or keep them in .gitmodules, but here lib/ seems to contain raw directories (since git submodules weren't initialized properly or user requested --no-git earlier).
# I'll leave lib/ tracked for now since it was installed without git submodules by user's instruction in the previous turn.